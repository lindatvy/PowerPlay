<%= form_for(misc) do |f| %>
  <% if misc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(misc.errors.count, "error") %> prohibited this misc from being saved:</h2>

      <ul>
      <% misc.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :image %>
    <%= f.text_area :image %>
  </div>

  <div class="field">
    <%= f.label :popup %>
    <%= f.text_area :popup %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
